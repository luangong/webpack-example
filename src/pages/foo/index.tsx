import styles from './styles.module.css';

// interface FooProps {
// }

export default function Foo(/* props: FooProps */) {
  return (
    // eslint-disable-next-line
    <div className={styles.foo}>foo page</div>
  );
}
